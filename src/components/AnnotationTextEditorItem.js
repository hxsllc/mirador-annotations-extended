import React, { Component } from 'react';
import PropTypes, { bool } from 'prop-types';

class AnnotationTextEditorItem {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>this is a texteditor item</div>
        )
    }
}
